let main = lambda v.
    printList (printList lambda x. print (showInt x)) case [Nil, [1], [2, 3]] of
        Cons a t =>
            (case t of
                Cons b t_ => Cons b (Cons a t_)
                | Nil => Cons a t)
        | Nil => Nil
